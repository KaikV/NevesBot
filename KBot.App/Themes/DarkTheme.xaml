<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:KBot.App.Controls">
  <SolidColorBrush x:Key="WindowBackground" Color="#FF0B1020" />
  <SolidColorBrush x:Key="SidebarBackground" Color="#FF10182B" />
  <SolidColorBrush x:Key="CardBackground" Color="#FF151F34" />
  <SolidColorBrush x:Key="ElevatedBackground" Color="#FF1B2941" />
  <SolidColorBrush x:Key="CardBorder" Color="#FF293A56" />
  <SolidColorBrush x:Key="PrimaryText" Color="#FFF3F7FF" />
  <SolidColorBrush x:Key="MutedText" Color="#FFA4B2C9" />
  <SolidColorBrush x:Key="AccentBrush" Color="#FF57DEF2" />
  <SolidColorBrush x:Key="SuccessBrush" Color="#FF70E0A8" />
  <SolidColorBrush x:Key="WarningBrush" Color="#FFFFCC76" />

  <Style TargetType="Button">
    <Setter Property="Background" Value="{StaticResource ElevatedBackground}" />
    <Setter Property="Foreground" Value="{StaticResource PrimaryText}" />
    <Setter Property="BorderBrush" Value="{StaticResource CardBorder}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Padding" Value="16,10" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="surface" Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                              Margin="{TemplateBinding Padding}" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="surface" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="surface" Property="Opacity" Value="0.42" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
    <Setter Property="Background" Value="{StaticResource AccentBrush}" />
    <Setter Property="Foreground" Value="#FF0B1020" />
    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
    <Setter Property="FontWeight" Value="SemiBold" />
  </Style>

  <Style TargetType="TextBox">
    <Setter Property="Background" Value="{StaticResource ElevatedBackground}" />
    <Setter Property="Foreground" Value="{StaticResource PrimaryText}" />
    <Setter Property="BorderBrush" Value="{StaticResource CardBorder}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Padding" Value="10,8" />
    <Setter Property="CaretBrush" Value="{StaticResource AccentBrush}" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
  </Style>

  <Style x:Key="ToggleOptionStyle" TargetType="CheckBox">
    <Setter Property="Foreground" Value="{StaticResource PrimaryText}" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="CheckBox">
          <Grid MinHeight="28">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="52" /></Grid.ColumnDefinitions>
            <ContentPresenter VerticalAlignment="Center" TextElement.Foreground="{TemplateBinding Foreground}" />
            <Border x:Name="state" Grid.Column="1" Width="48" Height="23" CornerRadius="4"
                    Background="{StaticResource ElevatedBackground}" BorderBrush="{StaticResource CardBorder}" BorderThickness="1">
              <TextBlock x:Name="stateText" Text="OFF" Foreground="{StaticResource MutedText}"
                         FontSize="10" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="state" Property="Background" Value="#FF0E6B4A" />
              <Setter TargetName="state" Property="BorderBrush" Value="{StaticResource SuccessBrush}" />
              <Setter TargetName="stateText" Property="Text" Value="ON" />
              <Setter TargetName="stateText" Property="Foreground" Value="{StaticResource PrimaryText}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Opacity" Value="0.45" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="{x:Type controls:CardControl}">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type controls:CardControl}">
          <Border Background="{StaticResource CardBackground}" BorderBrush="{StaticResource CardBorder}"
                  BorderThickness="1" CornerRadius="12" Padding="20">
            <StackPanel>
              <TextBlock Text="{TemplateBinding Title}" Foreground="{StaticResource MutedText}"
                         FontSize="11" FontWeight="Bold" Margin="0,0,0,14" />
              <ContentPresenter ContentSource="Content" />
            </StackPanel>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
